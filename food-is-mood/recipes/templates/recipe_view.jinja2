<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="shortcut icon" href="{{request.static_url('recipes:static/favicon.ico')}}" />
    <link rel="stylesheet" href="{{request.static_url('recipes:static/app.css')}}"/>
    <title>Food is Mood - {{ page.title }}</title>
</head>
<body>
  {% include 'components/nav.jinja2' %}

  <div class="content">
    <h1 class="recipe-title">{{page.title}}</h1>
    <span class="recipe-ranking">{% for star in range(page.rank) %}* {% endfor %}</span>
    <p>{{page.description|safe}}</p>
    <h2>Ingredients</h2>
    <ul class="ingredients">
    {% for ingredient in page.ingredients %}
      <li>{{ingredient.ingredient}}</li>
    {% endfor %}
    </ul>
    <h2>Steps</h2>
    <ol class="steps">
    {% for step in page.steps %}
      <li>{{step.step}}</li>
    {% endfor %}
    </ol>
  </div>
</body>
</html>
