<nav class="nav-main">
  <div class="brand">
    <a href="/" class="header">Food&nbsp;is&nbsp;Mood</a>
  </div>
  <ul>
    {% for link in links %}
      {% if link.uid %}
        <li><a href="{{ request.route_url(link.route_name, uid=link.uid) }}">{{ link.title }}</a></li>
      {% else %}
        <li><a href="{{ request.route_url(link.route_name) }}">{{ link.title }}</a></li>
      {% endif %}
    {% endfor %}
  </ul>
</nav>
